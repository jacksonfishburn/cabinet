spring.application.name=cabinet

server.port=${CABINET_PORT:8765}

cabinet.token=${CABINET_TOKEN}
cabinet.storage-dir=${CABINET_STORAGE_DIR:/cabinet-storage}
cabinet.max-size-bytes=${CABINET_MAX_SIZE_BYTES:104857600}